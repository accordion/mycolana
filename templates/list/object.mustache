        <p>{{title}}</p>
        {{>pagination}}

            {{#objects}}
            <li>
                <strong><a href="{{uri}}">{{id}}:</strong> ({{obinv}} : {{obobject}})</a>
                <div class="measurements">
                    <button class="addMeasurement">Add Measurement</button> 
                    <input type="hidden" name="object_id" value="{{id}}" /><br />
                   
<hr />
                    {{#measure}}
                        Value: {{value}}<br />
                    {{/measure}}
                </div>
            </li>
            {{/objects}}
